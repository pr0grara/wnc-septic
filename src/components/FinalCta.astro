---
/**
 * FinalCta.astro — bottom call/quote band.
 */
import { SITE, tel } from '@config/site';

interface Props {
  heading?: string;
  sub?: string;
}
const {
  heading = `Septic Trouble in ${SITE.region}? Let’s Sort It Out.`,
  sub = 'Call now for a fast quote — we come to your property, and backups and emergencies get priority.',
} = Astro.props;
---

<section class="final-cta">
  <div class="container">
    <h2>{heading}</h2>
    <p>{sub}</p>
    <div class="cta-actions">
      <a class="btn btn-call btn-lg" href={tel()}>📞 Call {SITE.phoneDisplay}</a>
      <a class="btn btn-outline btn-lg" href="#quote">Request a Quote</a>
    </div>
  </div>
</section>

<style>
  .final-cta {
    background: var(--navy);
    color: #eef3f8;
    padding: 64px 0;
    text-align: center;
  }
  .final-cta h2 {
    color: #ffffff;
  }
  .final-cta p {
    color: rgba(238, 243, 248, 0.92);
    max-width: 56ch;
    margin: 0 auto 24px;
    font-size: 1.1rem;
  }
  .cta-actions {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
  }
</style>
